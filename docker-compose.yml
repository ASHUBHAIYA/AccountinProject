version: '3'
services:
  accounting-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: accounting-app
    ports:
      - "80:5000"     # map port 80 → app runs on port 5000
    restart: unless-stopped
    env_file:
      - .env
